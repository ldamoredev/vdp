import type { RecurringTransaction as RecurringTransactionDto } from "@vdp/shared";

/** Read model for a recurring rule. Plain data — reuse the wire shape. */
export type RecurringTransaction = RecurringTransactionDto;
